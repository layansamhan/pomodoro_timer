name: pomodoro_timer_app
description: "A simple productivity tool that uses timed work sessions and short breaks to help you stay focused and avoid burning out."

publish_to: 'none' 



environment:
  sdk: ^3.11.3


dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^6.0.0


flutter:

  
  uses-material-design: true
